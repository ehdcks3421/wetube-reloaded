extends base


block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="Name", name="name",type="text", reqruied)
        input(placeholder="Email", name="email", type="email", reqruied)
        input(placeholder="Username", name="username", type="text", reqruied)
        input(placeholder="Password", name="password",type="password", reqruied)
        input(placeholder="Confirm Password", name="password2",type="password", reqruied)
        input(placeholder="Location", name="location",type="text", reqruied)  
        input(type="submit", value="Join")          
